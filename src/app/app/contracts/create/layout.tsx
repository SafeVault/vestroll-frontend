import type { Metadata } from "next";
export { default } from "../../../(app)/contracts/create/layout";

export const metadata: Metadata = {
  title: "Create Contract",
  description: "Create contracts easily",
};


